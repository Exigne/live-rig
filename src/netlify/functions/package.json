{
  "name": "live-rig",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev":   "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react":     "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.2"
  },
  "netlifyFunctions": {
    "dependencies": {
      "@neondatabase/serverless": "^0.9.5",
      "bcryptjs":   "^2.4.3",
      "jsonwebtoken": "^9.0.2",
      "cloudinary": "^2.4.0"
    }
  },
  "comments": {
    "note1": "Netlify Functions run in Node.js (CommonJS). Their dependencies are listed under netlifyFunctions.dependencies above.",
    "note2": "Run: cd netlify/functions && npm init -y && npm i @neondatabase/serverless bcryptjs jsonwebtoken cloudinary",
    "note3": "Or add a netlify/functions/package.json with those four packages."
  }
}
